const btn = document.querySelectorAll('.btn-carrinho')
btn.forEach((item) => {
    item.addEventListener('click', () => {
        alert('Carrinho ainda não está funcionando. Compras somente na loja :(')
    })
})